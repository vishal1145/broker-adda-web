import { ReactNode } from 'react';

export const metadata = {
  title: 'Queries Management | Manage Your Real Estate Queries | Broker Gully',
  description: 'Manage and track all your real estate leads in one place. View query details, update status, add notes, and connect with potential clients through Broker Gully.',
};

export default function LeadsLayout({ children }: { children: ReactNode }) {
  return children;
}

